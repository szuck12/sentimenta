[pytest]
pythonpath = .
testpaths = tests
addopts = -ra
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    model: real Hugging Face model tests (requires downloaded weights)
